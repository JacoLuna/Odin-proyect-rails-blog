<h1>Articles</h1>

<ul>
  <% @articles.each do |article| %>
    <li>
      <%#= article.title %>
      <!--<a href="/articles/<%= article.id %>"> -->
      <%# with helper method %>
      <a href="<%= article_path(article) %>"> 
        <%= article.title %>
      </a>
    </li>
  <% end %>
</ul>

<h1>Articles</h1>
<%# with more complex helpers %>
<ul>
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, article %>
    </li>
  <% end %>
</ul>


<%= link_to "New Article", new_article_path %>
